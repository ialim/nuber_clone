type verification {
  id: Int!
  target: String!
  payload: String!
  key: String!
  verified: Boolean!
  # used: Boolean!
  # user: User
  createdAt: String!
  updatedAt: String
}

type Query {
  verification: verification
}
